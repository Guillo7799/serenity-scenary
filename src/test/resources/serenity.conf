drivers {
    windows {
    webdriver.chrome.driver = src/test/resources/webdriver/windows/chromedriver.exe
    webdriver {
    driver = chrome
        capabilities {
            browserName = "chrome"
            "goog:"chromeOptions { args = ["start-maximized","remote-allow-origins=*","disable-download-notification",
            "allow-running-insecure-content", "ignore-certificate-errors", "test-type", "incognito",
            "no-sandbox","always-authorize-plugins","disable-web-security" ...] }
        }
       }
       }
       }
